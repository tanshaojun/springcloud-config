spring:
  profiles:
    active:
    - dev
---
spring:
  profiles: dev
  application:
    name: consul-server
  cloud:
    consul:
      host: localhost
      port: 8500
server:
  port: 3001
---
spring:
  profiles: dev
  application:
    name: consul-server
  cloud:
    consul:
      host: localhost
      port: 8500
server:
  port: 3002